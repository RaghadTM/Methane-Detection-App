=== RANDOM FOREST CONTEXTS ===
MATCH:  using a microcontroller with an integrated analog-to-digital converter (ADC). The digital data is processed and analyzed using an AI-based classification algorithm to determine whether a methane leak is present. The processed signal is then analyzed through a supervised machine learning algorithm (Random Forest classifier) that has been trained on labeled methane and non-methane signal patterns. The algorithm assesses the extracted features, including the level of signal attenuation and the rate of signal change, to effectively distinguish between real methane leaks and natural environmental variations. T

=== ISOLATION FOREST CONTEXTS ===
MATCH: ng solely on fixed threshold values, sensor data is analyzed to identify patterns associated with normal operation and leak events. Features such as signal magnitude variation and temporal behavior are extracted from the infrared measurements. An unsupervised anomaly detection approach based on the Isolation Forest algorithm is applied to model normal operating behavior and detect deviations indicative of methane leakage. Instead of relying on labeled leak data, the model learns baseline signal patterns and assigns an anomaly score to new observations based on structural deviation in the feature space. The ro

MATCH: es detection robustness, enhances early warning capability, and reduces false positives caused by transient noise or isolated fluctuations. 4.2.3.3 AI Classification and Decision Logic The processed and feature-engineered signal is analyzed using an unsupervised anomaly detection model based on the Isolation Forest algorithm. Unlike supervised classification approaches that require labeled leak and non-leak data, Isolation Forest is trained primarily on normal operating behavior and identifies deviations from that baseline as anomalies. This design choice is motivated by the nature of methane leak detection i

MATCH: ments, where leak events are rare and early-stage leak signatures may not follow consistent labeled patterns. By modeling normal system behavior, the AI subsystem can detect subtle deviations in absorbance trends, signal variability, and temporal dynamics that precede confirmed leak conditions. The Isolation Forest model operates by recursively partitioning the multidimensional feature space. Data points that require fewer partitions to isolate are assigned higher anomaly scores, indicating abnormal behavior. Because the algorithm evaluates structural deviation rather than fixed thresholds, it is well-suited 

MATCH: itical thresholds. Early warning states allow intervention prior to confirmed leak escalation, enhancing safety and reducing response time. The model is deployed within the FastAPI backend, where inference is performed in real time upon receipt of sensor data. Due to the computational efficiency of Isolation Forest, inference latency remains significantly below the system’s real-time requirement (≤ 2 seconds), ensuring timely alert generation without imposing excessive processing load. By combining time-series feature analysis with anomaly scoring and graded alert thresholds, the AI subsystem enhances detecti

MATCH: end serves as the computational interface between the sensing subsystem and the cloud infrastructure. Upon receiving securely transmitted sensor data via HTTPS, the backend performs the following operations: • Validates incoming requests • Executes feature extraction (if not precomputed) • Performs Isolation Forest inference • Computes anomaly scores • Maps scores to operational states (Normal, Yellow, Red) • Structures time-stamped detection records 63 By centralizing inference within the backend, the system allows model updates and parameter tuning without requiring firmware modifications in the embedded ha

MATCH: n subsystem, real-time backend processing, alert classification logic, and secure cloud communication of the integrated methane leak detection system. The CS subsystem consists of the following functional blocks: • Dataset Generation & Preprocessing • Feature Extraction Layer • Predictive AI Model (Isolation Forest) • Multi-Level Alert Classification • Backend API & Real-Time Processing • Secure Communication (HTTPS/TLS) • Firebase Firestore Integration 4.3.3.1 Dataset Generation & Preprocessing Because real industrial methane leak datasets were unavailable, a physics-based synthetic dataset was generated usi

MATCH: ted as: 𝐴= ln (𝐼0 𝐼𝑡 ) The dataset simulates: • Gradual methane concentration ramp-up • Steady leak phases • Decay behavior • Gaussian sensor noise • Baseline drift Training is performed using only normal operational data to align with anomaly detection methodology. This approach is consistent with Isolation Forest design principles, which model normal behavior and identify deviations as anomalies (Liu, Ting, & Zhou, 2008). This methodology enables predictive detection rather than reactive binary classification. The synthetic methane dataset used for model training and evaluation is accessible at: https://doc

MATCH: dependent feature analysis. The following features are computed: First derivative: 𝑑𝐴 𝑑𝑡 Rolling mean: 𝜇𝑡= 1 𝑛∑𝐴𝑖 𝑡 𝑖=𝑡−𝑛 Rolling variance: 75 𝜎𝑡 2 = 1 𝑛 ∑(𝐴𝑖−𝜇𝑡)2 These features capture early drift, volatility, and instability that may precede confirmed methane leaks. 4.3.3.3 Predictive AI Model – Isolation Forest The predictive model uses Isolation Forest, an unsupervised anomaly detection algorithm designed to isolate abnormal points through recursive partitioning of feature space (Liu et al., 2008). The anomaly score is defined as: 𝑠(𝑥, 𝑛) = 2 −𝐸(ℎ(𝑥)) 𝑐(𝑛) Where: • h(x) represents path length • c(n) is a

MATCH: ning complexity: 𝑂(𝑡 ∙ 𝑛log 𝑛) Inference complexity: 𝑂(𝑡 log 𝑛) 76 Where: • t = number of trees • n = subsample size Due to its logarithmic inference complexity, the model satisfies the real-time constraint of ≤ 2 seconds. In the implemented configuration, inference time is below 100 ms per window. Isolation Forest was selected because: • Leak events are rare (unsupervised problem) • Early-stage deviations are subtle • The algorithm is computationally efficient • It supports anomaly scoring rather than strict binary output • It is widely validated for anomaly detection tasks (Liu et al., 2008) 4.3.3.5 Multi-L

MATCH: f: 𝑠(𝑥) ≥𝑇2 ⇛ 𝐶𝑜𝑛𝑓𝑖𝑟𝑚𝑒𝑑 𝐿𝑒𝑎𝑘 (𝑅𝑒𝑑) This graded threshold approach enables predictive early warnings prior to confirmed leak detection. 77 4.3.3.6 Backend API & Real-Time Processing The backend is implemented using FastAPI (Python). It performs: • Secure HTTPS request handling • Feature extraction • Isolation Forest inference • Anomaly scoring • Alert classification • Database storage Server-side inference ensures centralized model control and eliminates the need for firmware updates during model improvements. 4.3.3.7 Secure Communication Layer (HTTPS/TLS) Data transmission occurs via HTTPS secured using Trans

MATCH: f Professional Engineers (NSPE). NSPE Code of Ethics for Engineers. https://www.nspe.org/resources/ethics/code-ethics [19] ISO 45001 – Occupational Health and Safety Management Systems. https://www.iso.org/iso-45001-occupational-health-and-safety.html 91 [20] F. T. Liu, K. M. Ting, and Z.-H. Zhou, “Isolation Forest,” in IEEE International Conference on Data Mining (ICDM), 2008. Available: https://ieeexplore.ieee.org/document/4781136 [21] E. Rescorla, “The Transport Layer Security (TLS) Protocol Version 1.3,” RFC 8446, 2018. Available: https://datatracker.ietf.org/doc/html/rfc8446 92 Appendix C Detailed Final 

MATCH: clearly define the function of each CS unit within the system architecture. The Data Acquisition section now explicitly explains the EE–CS interface and absorbance transformation. The Signal Processing section details preprocessing and temporal feature extraction. The AI section clearly defines the Isolation Forest anomaly detection model and multi-level alert logic (Normal, Yellow, Red). The Backend section specifies real-time inference responsibilities, while the Security section clarifies TLS-based encrypted communication. Each unit now has a defined functional role within the predictive methane detection 

MATCH:  management while maintaining confidentiality and integrity of transmitted data. Comment 3: Specifications Attainment There is a lack of design around your specifications and constraints Response: The CS sections have been revised to explicitly connect design decisions to system specifications. The Isolation Forest model was selected due to its computational efficiency and suitability for rare-event anomaly detection, ensuring real-time inference within the ≤2 second constraint. The predictive multi-level alert logic supports early warning capability as required by system safety objectives. HTTPS-based encryp

=== SVM CONTEXTS ===
